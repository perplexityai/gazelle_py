name: module-release

# Releases the `gazelle_py` Bazel module to GitHub Releases and submits it
# to the Bazel Central Registry.
#
# Tag scheme: `vX.Y.Z`.
#
# The release uses bazel-contrib's reusable workflows, pinned by commit SHA.
# BCR validates their builder IDs in the generated attestations, so local
# vendored copies would fail the registry presubmit.
#
# Prerequisites for the BCR submission to succeed:
#   1. A fork of github.com/bazelbuild/bazel-central-registry under the org
#      (matches `registry_fork` below).
#   2. The `bazel-contrib/publish-to-bcr` GitHub App installed on the fork.
#   3. Repo secret `BCR_PUBLISH_TOKEN` — a classic PAT (fine-grained PATs
#      cannot open PRs against public repos). See
#      https://github.com/bazel-contrib/publish-to-bcr#a-note-on-release-automation

on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag to release (e.g. v0.1.0). Must already exist.'
        type: string
        required: true

permissions:
  actions: read
  attestations: write
  contents: write
  id-token: write

jobs:
  release:
    uses: bazel-contrib/.github/.github/workflows/release_ruleset.yaml@af28703814cf26522cf5e44d219efece91293123 # v7.4.0
    with:
      release_files: gazelle_py-*.tar.gz
      prerelease: false
      tag_name: ${{ inputs.tag || github.ref_name }}

  publish-to-bcr:
    needs: release
    uses: bazel-contrib/publish-to-bcr/.github/workflows/publish.yaml@0a23c53c2baffdaf2ce8dd23c2c0e45eb3b79093 # v1.2.0
    with:
      tag_name: ${{ inputs.tag || github.ref_name }}
      registry_fork: perplexityai/bazel-central-registry
      draft: false
    permissions:
      actions: read
      attestations: write
      contents: write
      id-token: write
    secrets:
      publish_token: ${{ secrets.BCR_PUBLISH_TOKEN }}
